name: Pull Request

on:
  pull_request:
    branches: [main, canary]

permissions:
  contents: read

jobs:
  pr-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        job: [build, test, typecheck]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install Dashboard & Build GUI
        run: |
          cd dashboard && npm ci
          cd dashboard && npm run generate-routes
          cd dashboard && npm run build

      - name: Run Build
        if: matrix.job == 'build'
        run: make build-daemon

      - name: Run Tests
        if: matrix.job == 'test'
        run: go test -v -race ./...

      - name: Run Typecheck
        if: matrix.job == 'typecheck'
        run: cd dashboard && npm run typecheck
