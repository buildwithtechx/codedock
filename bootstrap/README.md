# Codedock Bootstrap Scripts

Canonical location for Codedock installation and node setup scripts.

## Scripts

- `install.sh`: Main installation script for Codedock control plane.
- `worker.sh`: Node setup script for worker nodes.
- `install-cli.sh`: Standalone CLI installer.
- `codedockd`: Systemd unit file wrapper for daemon service.
